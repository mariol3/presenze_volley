== Presenze Volley

Simple web application to manage trainings of my volley ball team while learning Rails.

== Installation
* Clone the project;

* Run <code>bundle</code> to install all required gems;

* Run <code>rake db:migrate</code> to apply all db migrations;

* Run <code>rake db:seed</code> to create an admin user to start using the app.